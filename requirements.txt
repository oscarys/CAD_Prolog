# CAD Prolog System — Python dependencies
# Tested with Python 3.12
#
# Install:
#   pip install -r requirements.txt
#
# SWI-Prolog must be installed separately (NOT via pip):
#   Ubuntu/Debian:  sudo apt install swi-prolog
#   macOS:          brew install swi-prolog
#   Windows:        https://www.swi-prolog.org/download/stable
#
# PySwip links against the SWI-Prolog shared library at runtime.
# If you get "libswipl not found", set:
#   export LD_LIBRARY_PATH=/usr/lib/swi-prolog/lib/x86_64-linux  (Linux)
#   export LIBSWIPL_PATH=/opt/homebrew/lib/libswipl.dylib        (macOS)

# Core bridge
pyswip==0.3.3

# Web application
Flask==3.1.3
Flask-Session==0.8.0

# Testing
pytest>=7.0

# Optional: better session backend for multi-user deployments
# redis>=5.0
# Flask-Session[redis]
